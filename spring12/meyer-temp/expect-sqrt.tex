want RV's S & T s.t.

\expect{S} \neq \expect{T}
\expect{\sqrt{S}} = \expect{\sqrt{T}}

(easy to have \expect{S} = \expect{T}=0 and \expect{S^2} \neq
\expect{T^2}: let $S$ be 1,-1 valued, $T$ 2,-2 valued)


prolly this requires \variance{S} = \variance{T} = \infty, but not
sure

ARM 5/9/11


\pdf_S(n) \eqdef

\pdf_T(n) \eqdef
