\documentclass[problem]{mcs}

\begin{pcomments}
    \pcomment{CP_GCD_algebra}
    \pcomment{by Shri Ganeshram March 2012}
    \pcomment{edited by ARM 3/13/12}
\end{pcomments}

\pkeywords{
  GCD
  Euclidean algorithm
}


\begin{problem}
Use the Euclidean Algorithm to prove that
\[
\gcd(13a+8b, 5a+3b) = \gcd(a, b).
\]

\begin{solution}
\begin{align*}
\gcd(13a+8b, 5a+3b)
   & = \gcd(5a+3b,3a+2b)
          & (\text{since } 13a+8b = 2\cdot (5a+3b) + (3a+2b)\ )\\ 
   & = \gcd(3a+2b, 2a+b)
          & (\text{since } 5a+3b = 1\cdot (3a+2b) + (2a+b)\ )\\
   & = \gcd(2a+b, a+b)
          & (\text{since } 3a+2b = 1\cdot (2a+b) + (a+b)\ )\\
   & = \gcd(a,a+b)
          & (\text{since } 2a+b = 1\cdot (a+b) + a\ )\\
   & = \gcd(a,b)
          & (\text{since } a+b =   1\cdot a+ b\ )
\end{align*}

\end{solution}

\end{problem}

\endinput
