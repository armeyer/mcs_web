\documentclass[problem]{mcs}

\begin{pcomments}
  \pcomment{FP_logical_jections}
  \pcomment{\textbf{Injections and Cardinality.}}
  \pcomment{ARM may 2013 based on F99.final}
  \pcomment{too tricky for final; use for pset}
\end{pcomments}

\pkeywords{
  injection
  total
  function
  composition
  inverse
}
\begin{problem}

\begin{staffnotes}
1 pt each
\end{staffnotes}

Let $R$ be a binary relation on a set $D$.  Let $x,y$ be variables
ranging over $D$.  Circle the expressions below whose meaning is that
$R$ is an \emph{injection} $[\leq 1\ \text{in}]$.  Remember $R$ is a not
necessarily total or a function.

\begin{enumerate}
%\item $R(x) \intersect R(y) = \emptyset$
\item $R(x) = R(y) \QIMPLIES x = y$
\item $R(x) \intersect R(y) = \emptyset \QIMPLIES x \neq y$
\item $R(x) \intersect R(y) \neq \emptyset \QIMPLIES x \neq y$
\item\label{RxintRyneq} $R(x) \intersect R(y) \neq \emptyset \QIMPLIES x = y$
\item $\inv{R}(R(x)) = \set{x}$
\item\label{invRRsubsx} $\inv{R}(R(x)) \subseteq \set{x}$
\item $\inv{R}(R(x)) \supseteq \set{x}$
\item $R(\inv{R}(x)) = x$
%\item $\QNOT(\exists x.\, \exists y \neq x.\, R(x) \intersect R(y) \neq \emptyset)$
\end{enumerate}

\begin{solution}
Only~\ref{RxintRyneq} and~\ref{invRRsubsx}.
\end{solution}
\end{problem}

\endinput
