\documentclass[problem]{mcs}

\begin{pcomments}
  \pcomment{MQ_predicate_jections_alt} 
  \pcomment{variant of MQ_predicate_jections_F13} 
  \pcomment{DIG 10/8/17}
\end{pcomments}

\pkeywords{
  surjection
  bijection
  injection
  predicate
  formula
  total  
  relation
  function
}

\begin{problem}

Five basic properties of binary relations $R:A \to B$ are:

\begin{enumerate}

\item\label{assert:surj} $R$ is a surjection $[\geq 1\ \text{in}]$

\item\label{assert:inj} $R$ is an injection $[\leq 1\ \text{in}]$

\item\label{assert:func} $R$ is a function $[\leq 1\ \text{out}]$  %\geq typo corrected

\item\label{assert:total} $R$ is total $[\geq 1\ \text{out}]$

\item\label{assert:empty} $R$ is empty $[= 0\ \text{out}]$

%\item $R$ a bijection$[= 1\ \text{in,out}]$\hfill\examrule[1in]

\end{enumerate}

Below are some assertions about a relation $R$.  For each assertion,
write the numbers of all the properties above that the relation $R$
must have; write ``none'' if $R$ might not have any of these
properties.  For example, you should write ``\ref{assert:surj},
\ref{assert:total}'' next to the first assertion.

Variables $a,a_1,\dots$ range over $A$ and $b,b_1,\dots$ range over
$B$. You may assume $A$ and $B$ each have at least $2$ elements.

\bparts

\ppart $\forall a, b.\ \ a \mrel{R} b$.\hfill\examrule[0.4in]\underline{\ref{assert:surj},
    \ref{assert:total}}\examrule[0.4in]

\ppart $\forall a, b_1, b_2.\ \ [(a \mrel{R} b_1 \QAND a \mrel{R} b_2) \QIMPLIES b_1 = b_2]$.\hfill\examrule[1.0in]
\begin{solution}
function \ref{assert:func}
\end{solution}

\ppart $\exists a \forall b.\ \ (a \mrel{R} b)$.\hfill\examrule[1.0in]
\begin{solution}
surjection \ref{assert:surj}
\end{solution}

\ppart $\QNOT [\exists a, b_1, b_1. \ \ (a \mrel{R} b_1 \QOR a \mrel{R} b_2)]$.\hfill\examrule[1in]
\begin{solution}
empty \ref{assert:empty}
\end{solution}

\ppart $R^{-1}$ is a function.\hfill\examrule[1in]
\begin{solution}
injection \ref{assert:inj}
\end{solution}

\ppart $A = B \QAND \forall a. \ \ a \mrel{R} a$.\hfill\examrule[1in]
\begin{solution}
total surjection  \ref{assert:surj}, \ref{assert:total}
\end{solution}


\ppart $\exists a, b. \ \ a \mrel{R} b$.\hfill\examrule[1in]
\begin{solution}
none
\end{solution}

\ppart $\forall a_1 \exists b. \ \ [a_1 \mrel{R} b \QAND (\forall a.\ \  a \mrel{R} b \QIMPLIES a = a_1)]$.\hfill\examrule[1in]
\begin{solution}
total injection \ref{assert:inj}, \ref{assert:total}
\end{solution}

\eparts

\end{problem}

\endinput
