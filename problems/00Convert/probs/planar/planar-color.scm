;; planar-color

(define-problem
  '(
    (type short-answer)
    (title "Faces of a Planar Embedding")
    (box-width 20)
    (questions
     (! "<strong>How many faces are there in the following graph?</strong>"
 (answer (3) (compare test-set)
          (rationale 
"

"))  ) ;!
     ) ;questions
    ) ;'
  );define-problem
